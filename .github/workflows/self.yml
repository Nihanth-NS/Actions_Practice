name: Adding for self hosted server
on:
    push:
        branches: ['master']
jobs:
    First:
        runs-on: self-hosted
        steps:
        - name: Create Dir
          run: mkdir IAM
        - name: Create File inside Dir_IAM
          run: |
               cd IAM
               touch Hello.txt
          